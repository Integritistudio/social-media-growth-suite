# Backend API (used for Next.js /api rewrites and OAuth connect redirects)
NEXT_PUBLIC_API_URL=http://localhost:3001
# LinkedIn app Client ID (display only; OAuth secret stays on server)
NEXT_PUBLIC_CLIENT_ID=your_linkedin_app_client_id
